# Day 0 verification needs only these two.
requests>=2.31.0        # raw HTTP calls, so we SEE the Comtrade API
python-dotenv>=1.0.0    # loads the git-ignored .env so our key never touches code

# Added in later days:
# comtradeapicall       # official UN client, used for bulk pulls (Day 2)
# pandas                # transform layer (Day 3)
# pyarrow               # write partitioned Parquet (Day 3)
# boto3                 # AWS SDK for Python (Day 1+)
